server:
  port: 8080

spring:
  application:
    name: java-context-service
  jackson:
    serialization:
      indent-output: true

app:
  parse-cache:
    enabled: true
    dir: artifacts
    expire-after-access-minutes: 5

logging:
  level:
    ru.kalinin.context: DEBUG
